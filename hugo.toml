baseURL = "https://example.org/"
languageCode = "en-us"
title = "My Zero-Cost JAMstack Site"


[build]
  writeStats = true

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[outputs]
  home = ["HTML", "RSS"]
  page = ["HTML"]

[caches]
  [caches.images]
    dir = ":cacheDir/images"